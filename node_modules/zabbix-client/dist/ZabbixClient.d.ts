import { ZabbixAPI } from './ZabbixAPI';
export declare class ZabbixClient {
    private url;
    constructor(url: string);
    login(username: string, password: string, relogin?: boolean): Promise<ZabbixAPI>;
    private ajustLogin;
}
