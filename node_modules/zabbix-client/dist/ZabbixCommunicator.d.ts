import { ZabbixSocket } from './ZabbixSocket';
export declare class ZabbixCommunicator {
    private socket;
    protected method: string;
    constructor(socket: ZabbixSocket, method: string);
    call<T>(params?: any, noAuth?: boolean): Promise<T>;
}
